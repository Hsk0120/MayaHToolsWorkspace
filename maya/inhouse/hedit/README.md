# hedit

Maya用のPython／MELスクリプトエディタです。VS CodeのDark+を参考にした配色、タブ、行番号、補完候補、出力欄を備えています。hlibとは独立しています。

## 起動

0.2.2では上部ツールバーをMaya標準スクリプトエディタと同じアイコン中心の表示に変更しました。開く・保存、出力/入力/両方の消去、出力のみ/入力のみ/両方の表示、全体実行/選択実行、Explorerを配置しています。マウスを重ねると操作名とショートカットを表示します。入力の消去はCtrl+Zで戻せますが、出力の消去は戻せません。

各アイコンの操作はFile・Edit・History・View・Commandメニューからも選べます。Refresh completionはCommandメニューに移動しました。実行のCtrl+Enter／F5は従来どおりです。標準エディタの全機能を複製するものではなく、コードと出力のDark+配色も維持しています。

ワークスペースの起動バッチでMayaを起動し、Pythonタブで実行します。

```python
import hedit
hedit.show()
```

`maya/modules/hedit.mod` がPythonパスとバージョン別プラグインパスを設定します。通常は `show()` または前回開いていた画面の起動復元時にロードします。Plugin Managerで明示ロードした場合もWindowメニュー末尾に起動項目を追加します。緑のHアイコンが目印です。起動済みのMayaには、`.mod` を追加した後の再起動が必要です。

プラグインの信頼確認が出た場合はMayaの画面で確認してください。heditはSecurity設定を変更しません。

0.1.3では補完もMayaと同じプロセス内で実行します。旧版を起動済みの場合は、編集中のスクリプトとシーンを保存してMayaを再起動してください。C++プラグインはPythonの`reload()`だけでは更新されません。

初回はフローティング表示です。Mayaのドックタイトル部分をドラッグしてレイアウトへ配置できます。コードから下側へドッキングして開く場合は次のように指定します。

```python
hedit.show(floating=False)
```

`hedit.show(floating=True)`でフローティングに戻します。通常の`show()`は既存の配置を保持します。`MayaQWidgetDockableMixin`を継承したホストにC++編集画面を配置し、`workspaceControl`の復元用`uiScript`も登録します。

### 未保存タブの自動復元

0.1.6から、ファイルへ保存していないコードも自動保存します。次回のMayaで`hedit.show()`を実行すると、タブの本文・順序・ファイルパス・未保存状態・選択位置・アクティブタブを復元します。復元したコードは自動実行しません。

保存先は`cmds.internalVar(userPrefDir=True)`の下の`hedit/tabs.json`です。Mayaバージョンごとに分かれます。1秒間隔で変更を確認し、入力停止から1.5秒以上経過した時に安全にファイルを置き換え、終了通知・プラグイン解除時にも保存します。元の`.py`ファイルは上書きしません。外部で元ファイルが変更・削除された場合も、復元本文を未保存として残します。

タブの×やCtrl+Wは明示的な削除なので、未保存なら保存確認が出ます。Discardで閉じたタブは次回復元しません。エディタ全体を閉じる操作やプラグイン解除では、自動保存が成功すれば確認なしで内容を保持します。

同じ復元先を複数のMayaで使用する場合、最初に開いたheditだけが保存します。後から開いた側には出力欄で通知し、復元先を上書きしません。読み取れない復元ファイルも通知して元のまま保持します。保存エラーはステータス欄に表示します。復元保存が使えない場合、プラグイン解除では従来の保存確認に戻ります。

最後の自動保存以降の編集（連続入力中と入力停止直後は未保存）、テキストUndo履歴、出力ログは復元対象外です。旧版から更新する最初の再起動だけは、先に編集中のコードをファイルへ保存してください。

## 操作

| 操作 | キー／場所 |
| --- | --- |
| 補完候補 | 入力時、または Ctrl+Space |
| 候補の確定 | Enter／Tab |
| 選択範囲を実行（選択なしなら全体） | Ctrl+Enter |
| タブ全体を実行 | F5 |
| 新規／開く／保存 | Ctrl+N／Ctrl+O／Ctrl+S |
| 検索 | Ctrl+F |
| 補完情報の再取得 | Refresh completion |
| 出力の消去 | 出力欄を右クリック → Clear output、またはツールバー |

### 選択範囲の実行（Maya形式）

0.1.5ではコード欄が実行キーを直接受け取ります。更新前のプラグインを使用中ならMayaを再起動してください。

- コードをドラッグ、またはShift+矢印で選択し、Ctrl+Enterで選択した部分だけ実行します。Ctrl+Lで現在行を選択してから実行することもできます。
- 選択がない場合は現在タブ全体を実行します。カーソル行だけの実行ではありません。
- テンキーのEnter（Ctrl併用も可）でも同じ実行ができます。通常のEnterは改行です。
- 実行後もコードと選択範囲は保持します。F5／Run allは選択に関係なくタブ全体を実行します。
- 選択した文字列をそのままPythonとして実行します。関数・for文などは必要なブロック全体を選択してください。インデントの自動除去や不足した文の補完は行いません。

選択してCtrl+Enterで実行する操作は[AutodeskのScript Editor説明](https://help.autodesk.com/cloudhelp/2023/ENU/Maya-Scripting/files/GUID-7C861047-C7E0-4780-ACB5-752CD22AB02E.htm)を参考にしています。MELも実行できますが、標準Script Editorの全機能を再現するものではありません。

### VS Code系ショートカット（Windows）

0.1.4から対応しています。更新前のheditをロード済みの場合はMayaを再起動してください。heditにフォーカスがある間だけ有効です。編集操作はコード欄を対象にし、MayaシーンのUndoとは別のテキストUndoで戻せます。

| キー | 操作 |
| --- | --- |
| Ctrl+/ | 選択行／現在行のコメント切り替え（Pythonは#、MELは//） |
| Alt+↑ / ↓ | 選択行／現在行を移動 |
| Shift+Alt+↑ / ↓ | 選択行／現在行を複製 |
| Ctrl+Shift+K | 選択行／現在行を削除 |
| Ctrl+L | 現在行を選択。繰り返すと次の行まで拡張 |
| Tab / Shift+Tab | 選択行のインデント／解除（選択なしのTabは空白4文字） |
| Ctrl+] / Ctrl+[ | 現在行／選択行のインデント／解除 |
| Ctrl+C / Ctrl+X | 選択範囲をコピー／切り取り。選択なしなら現在行 |
| Ctrl+Z / Ctrl+Shift+Z | テキストUndo／Redo |
| Ctrl+F / Ctrl+H | 検索バー／置換バーを表示 |
| F3 / Shift+F3 | 次／前の一致箇所。末尾／先頭で折り返す |
| Escape（検索欄） | 検索バーを閉じてコードへ戻る |
| Ctrl+G | 操作中のコード欄・出力欄で指定行へ移動（行番号入力後Enter、Escで取消） |
| Ctrl+Shift+S | 名前を付けて保存 |
| Ctrl+W / Ctrl+F4 | 現在タブを閉じる（未保存なら確認） |
| Ctrl+Tab / Ctrl+Shift+Tab | 次／前のタブ |
| Ctrl+PgDown / Ctrl+PgUp | 次／前のタブ |
| Alt+Z | 折り返し表示の切り替え |

検索は現在タブ内を対象にします。検索欄へ1文字入力するごとに一致箇所と件数が更新され、Enterは不要です。入力を追加しても現在の一致を基準に絞り込みます。空欄にすると選択と件数表示を解除します。Enter/F3は次の一致へ移動します。0.2.6から入力欄の右上に重ねて表示し、Ctrl+Fで検索、Ctrl+Hまたは左端の`>`で置換欄を開きます。Escapeで閉じます。検索バーの`Tt`で大文字小文字の区別、`Abc`で単語単位、`.*`で正規表現を切り替えます（初期状態は全てオフ）。ステータス欄に一致番号と件数を表示します。置換バーのReplaceは現在の一致箇所、Replace allは現在タブ全体を対象にします。一括置換は1回のUndoで戻せます。

正規表現はQtのQRegularExpression方式です。正規表現モードの置換では`$1`〜`$99`をキャプチャー、`$&`を一致全体、`$$`をドル記号として扱います。通常モードは入力文字をそのまま使用します。名前付き参照や`\\1`記法は未対応です。不正な正規表現、長さ0の一致、10万件を超える一致は通知して処理を止め、置換しません。

### 表示倍率（0.1.10）

View → Zoom in / Zoom out / Reset zoom、またはCtrl+=（Ctrl++）／Ctrl+-／Ctrl+0でコード欄と出力欄の文字サイズを変更できます。コード欄は10〜28px（標準14px）、Outputは2px小さい8〜26px（標準12px）で、設定は次回起動に引き継ぎます。Maya標準のメニュー等のサイズは変更しません。

既存の実行操作を保つため、**Ctrl+Enterはスクリプト実行のまま**です。VS Codeの「下に行を挿入」とは異なります。F5もheditでは全体実行です。マルチカーソル（Ctrl+Dなど）、コード整形、定義ジャンプ、VS Code全機能のキー互換は未対応です。

キーの参考: [VS Code Windows公式ショートカット](https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf)。

0.1.2から、コードはMaya標準のPython実行経路で実行し、変数の名前空間も標準Script Editorと共有します。heditの実行結果はMaya共通の出力へ流れ、標準Script Editorにも表示されます。hedit側はMaya標準reporterの表示文書と`MCommandMessage`の種別通知を購読し、他のエディタからの出力・警告・エラーも表示します。stdout/stderrの置き換えは行いません。シーン操作のUndo可否は実行したコマンドに従い、自動のUndoチャンクは追加しません。

0.2.3から、初回表示時にMayaが保持する履歴を最大512Ki文字取り込みます。Maya起動時の出力も、保持されていれば表示します。その後は新しい出力通知を購読し、初期履歴と重複させません。Mayaですでに消去された履歴や、別エディタ独自のログは復元できません。Clear outputはheditの表示だけを消去します。hedit本体とスクリプト実行・補完はMayaと同じプロセスです。

Result／Warning／ErrorはMaya自身が整形した記号と改行を使用し、結果は緑、警告は黄、エラーは赤で表示します。Pythonの`print`にはコメント記号を追加しません。履歴の読み取りにはMaya標準の[cmdScrollFieldReporter](https://help.autodesk.com/cloudhelp/ENU/MayaCRE-Tech-Docs/CommandsPython/cmdScrollFieldReporter.html)を使用し、標準エディタの履歴・フィルター設定は変更しません。

補完候補の確定による本文変更では次の自動補完を予約しません。Enterで候補を確定した後、次のEnterで改行できます。

Maya 2024／2027のGUIで、標準Script Editorとの双方向出力、変数共有、日本語、警告・例外、出力が重複しないことを確認しています。CharcoalEditor2もMaya共通出力を使用する範囲では共有対象ですが、実物との連携テストは専用環境のプラグイン信頼確認で停止したため未確認です。信頼設定の自動変更は行いません。

出力欄を上、コードのタブを下に配置しています。中央の境界をドラッグして高さを調整できます。ファイルはUTF-8を使用します。通常のドックを閉じる操作では非表示になり、そのMayaセッション中はタブを保持します。

0.2.9から、Mayaメインスレッドのログ通知はタイマーを待たず最大約40fpsでOutputへ反映・再描画します。シーン読み込み中も通知に合わせて表示し、操作イベントを強制処理する`processEvents`は使用しません。最後の未描画分は25msタイマーで反映します。他スレッドからの出力はMaya自身のreporterがメインスレッドで文書へ反映するまで遅れる場合があります。Maya自身がまだ出力していない内容は表示できません。

## Outputの表示モード（0.2.7）

上段のアイコンバーでNormal（全種別）、Output only（実行コードのHistoryを除外）、Warnings + Errors、Errors onlyを切り替えます。保持中のログはモードを戻すと再表示します。Maya側のEcho Allや他エディタの設定は変更しません。初回履歴は警告・エラー・結果の接頭辞以外に種別情報がないため、過去の実行コードはOutput onlyでも表示される場合があります。

Output専用の見出しバーと保存・クリアボタンはありません。クリアは従来のアイコンバー、Historyメニュー、右クリックから操作でき、全モードの保持ログも消去します。保持上限は約100万文字、表示は最大5,000ブロックです。上限を超えて破棄した古いログは復元しません。表示モードの初期値はNormalです。

## Outputとスペルチェック（0.2.5）

Outputは標準12pxで、標準状態では折り返しません。横長の行は水平スクロールできます。Edit → Preferences → Wrap output linesで折り返しを切り替えます。起動履歴はMayaの表示用Qt文書から取得します。過去の通知区切りが改行に変換されているため、初回取り込みでは空行を省略し、空白だけの行を挟む断片（`optimization on`など）を接続します。この過去履歴では意図的な空行との区別や、区切り文字のない任意の分割出力の完全復元はできません。表示開始後のライブ出力にはこの圧縮を適用せず、分割printと意図的な空行を維持します。タイトルは文字化けを避けるASCII表記です。

Edit → Preferences → Spell check (English)で英語のスペル波線を切り替えます。標準はONです。Windows標準のen-US辞書を利用するため、外部Pythonライブラリ・辞書の同梱・オンライン送信は不要です。辞書が利用できない場合はステータスバーに表示します。

入力停止から450ms後に、アクティブなPython/MELタブの表示範囲（最大8,000文字）だけをチェックします。4〜40文字の英単語が対象で、camelCaseとsnake_caseを分割し、語単位で結果をキャッシュします。カーソル行の背景と青い波線を併用し、OFFにすると波線と保留中の検査を消します。Maya/Pythonの代表的な語は除外していますが、独自名には誤検出があり得ます。日本語の校正、修正候補、ユーザー辞書、cSpell設定ファイルの互換性は対象外です。文法エラーを調べるStatic analysisとは独立しています。

参考: [WindowsのスペルチェックAPI](https://learn.microsoft.com/en-us/windows/win32/api/spellcheck/nn-spellcheck-ispellchecker)。

## 外観（0.1.9）

メニュー・タブ・ツールバー・ステータスバーはMayaの標準Qtスタイルを継承します。出力上／コード下の配置を保ち、コード欄と出力欄だけをDark+配色・Consolas（コード14px、Output 12px）にしています。背景`#1e1e1e`、通常文字`#d4d4d4`、変数`#9cdcfe`、関数`#dcdcaa`、クラス`#4ec9b0`、文字列`#ce9178`、コメント`#6a9955`はhlibのSphinx `code-dark-plus.css`に合わせています。出力は通常文字色を基本とし、エラー行を赤、警告行を黄で区別します。

色の分類は簡易なPython構文強調のため、PygmentsやVS Codeの意味解析と完全に同じ色分けにはなりません。

### 出力の色（0.1.11）

0.1.12では出力欄をマウスやShift+矢印で選択し、Ctrl+Cでコピーできます。Ctrl+Aは出力欄の全選択、右クリックのCopyも使用できます。出力は読み取り専用で、貼り付けや文字の削除はできません。

0.2.1から出力欄の行番号はデフォルトで非表示です。Edit → Preferences → Show output line numbersで表示でき、設定は次回起動にも保持します。コード欄の行番号は常に表示します。行番号は桁数・文字サイズ・スクロールに追従し、コピーする本文には含まれません。非表示でもCtrl+Gの行移動は利用できます。出力をClearした場合や5,000行の上限で古い行が消えた場合は、残っている本文の先頭を1行目とします。

0.1.14では操作したいコード欄または出力欄をクリックし、Ctrl+Gで下部に行番号入力欄を開けます。1始まりの行番号を入力してEnterで移動、Escで取り消します。Maya 2024ではEsc後にフォーカスがビューポートへ戻る場合があるため、その場合はコード欄・出力欄をクリックして操作を続けてください。

ログの追記中も選択範囲とスクロール位置を保持します。選択がなく末尾を表示している場合だけ新しいログへ追従します。表示上限の5,000行を超えて削除された古いログや、Clear outputで消した文字列は保持対象外です。

0.2.10から、専用の非表示Maya reporterが整形した表示文書の追記だけを取得します。通常のPython/MELのprintはそのまま、情報通知は`// `、Pythonの警告・例外は`# Warning:`／`# Error:`など、標準Script Editorと同じ記号・改行になります。複数行の通知や空行にもMaya側の書式を適用し、独自の末尾` //`は追加しません。

MayaのMCommandMessageが通知する種別を保持して描画します。行頭の文字列だけでは判定しないため、複数行の警告・エラーでも種別に応じた色を維持し、続く通常出力に色が残りません。

| 種別 | 色 |
| --- | --- |
| Warning | 黄 `#ffff00` |
| Error（エラー通知に含まれるトレースバックも含む） | 赤 `#ff0000` |
| Result（Mayaが結果として通知した値） | 緑 `#b5cea8` |
| Info | 水色 `#9cdcfe` |
| History | グレー `#a0a0a0` |
| Display・通常のprint | 明るいグレー `#d4d4d4` |

`print("Error: ...")`や`print(result)`の文字列からエラー・成功を推測することはしません。独自ロガーが通常出力として送った警告も通常色になります。Maya標準エディタの内容や色、Echo All設定は変更しません。

## 補完の仕組み

- C++/Qtで編集・表示を行い、入力後100msでMaya内のPython補完関数を直接呼びます。別プロセス・IPC・ワーカーの起動待ちはありません。
- 現在の`sys.path`とロード済みモジュールの公開名を使用します。
- Python標準ライブラリ`ast`で未ロードのソースを解析します。補完対象をimport・実行しません。
- ソース解析はキャッシュし、更新時刻・サイズが変わったら再解析します。ロード済みの`maya.cmds`などでは実在名を優先し、検索パスを走査しません。

Jedi・Pyright等、追加のPythonライブラリは不要です。同一プロセスのメインスレッドで補完するため、大きなファイルや遅いネットワークパスの解析中はMayaの操作も待機します。旧版の別ワーカー停止によるタイムアウト機能はありません。

`import maya.cmds as cmds`の別名、`from package import Class`、モジュールの公開名、ソース上のクラスの直接定義メソッド、トップレベルのローカル関数を補完します。ロード済みの`hlib.ls`など動的な公開名も対象です。関数の引数名は候補のツールチップで確認できます。

0.1.7から、次の補完要求時に対象モジュールの現在の公開名と`sys.path`を取得します。`hlib.reload()`後や実行後のimportもRefresh completionなしで追従します。読み込み済みモジュールは既知の`.py`ファイルだけを更新日時・サイズで確認し、関数やクラスの直接定義も候補へ反映します。新しいトップレベルパッケージはimport補完時に同一プロセスのバックグラウンドスレッドで検出します（走査開始は最短5秒間隔）。走査中は既存候補を即時表示し、走査完了後の補完要求で新しい候補が使えます。Maya APIと公開名の取得はメインスレッドで行います。

補完候補の更新と、Mayaで実行するライブラリの更新は別です。ソース保存直後に新しい候補が出ても、実行にはライブラリ自身のreloadが必要な場合があります。heditは対象を自動import・reloadしません。読み込み済みの公開名は実際に存在する間は残ります。解析中の構文エラーでは読み込み済みモジュールの最後の正常な静的候補を維持します。強制的に解析キャッシュを作り直す場合は **Refresh completion** を押してください。

0.2.5では自動補完を入力停止から250ms後に実行します。宣言部分の解析をキャッシュし、構文エラー時の再試行を最大4回、補完対象をカーソルまで20万文字に制限します。全ロード済みモジュールの属性を起動時に一括JSON化する処理も廃止しました。スペルチェックは引き続きC++です。

## 編集・補完の設定

**Edit → Preferences**で次の項目を切り替えられます。設定は復元ファイルと同じフォルダーの`preferences.ini`へ保存し、次回起動にも適用します。

| 項目 | 動作／初期値 |
| --- | --- |
| Completion while typing | 文字入力時の自動補完。オン |
| Completion after dot | ドット入力時の自動補完。オン |
| Include Python keywords / built-ins | キーワード／組み込み名を候補へ含める。オン |
| Static analysis (syntax / warnings) | 編集中タブの構文診断。オフ |
| Smart indentation | Enter時にインデントを引き継ぎ、末尾がコロンなら4空白追加。オン |
| Backspace to indentation stop | 行頭の空白を4文字の区切りまで削除。オン |
| Show spaces and tabs | 空白・タブを可視化。オフ |
| Trim trailing spaces on file save | 明示的なファイル保存時に行末空白を削除。オフ |
| Ensure final newline on file save | 明示的なファイル保存時に末尾改行を追加。オフ |

自動補完を両方オフにしてもCtrl+Spaceによる手動補完は使えます。保存時の整形はタブ復元用の自動保存には適用しません。元ファイル保存が成功した時だけコード欄にも反映し、1回のテキストUndoで整形前へ戻せます。インデント幅は現在4空白固定です。

### 静的解析（0.1.8）

Edit → Preferences → **Static analysis (syntax / warnings)**でオン／オフを切り替えます。初期状態はオフで、設定は次回起動にも保持します。補完のオン／オフとは独立しています。

オンの場合、現在のタブで入力が止まってから0.8秒後に、Maya同梱Pythonのコンパイラで構文を確認します。対象コードの実行・import・reloadや、外部の解析パッケージは使用しません。問題一覧はコード欄の下に表示し、クリックすると該当行へ移動します。編集やタブ切り替え時に古い診断を消し、再解析します。オフにすると待機中の解析と表示を取り消します。

構文エラー、インデントエラー、関数外のreturn、一部のSyntaxWarningが対象です。最初の構文エラーを修正すると次を確認できます。型チェック、未定義変数、import先の存在、Maya APIの引数検証、Pylint相当の全ルールは対象外です。診断内容は各MayaのPythonバージョンに従います。

Mayaと同じプロセスで同期解析するため、解析中は短時間UIが待機します。100万文字超、または2万行以上は解析を省略します。この上限は実行時間の保証ではありません。重い場合はオフにしてください。

## 現段階の制限

- PythonとMELを実行できます。MEL補完・MEL静的解析、デバッガー、LSP、VS Code拡張機能は未対応です。
- 関数の戻り値や任意のインスタンスの型推論、継承メソッドの完全な解決、ネストしたスコープの解析は未対応です。
- zip内ソース、未ロードのバイナリ拡張、`__getattr__`で生成される未知の属性は完全には補完できません。
- 補完は静的な名前の候補であり、型・構文の正しさを保証するものではありません。
- スクリプト実行自体はMayaのメインスレッドで行うため、長時間の実行中はMayaが待機します。
- WindowsのMaya 2022（Python 3）／2024／2025／2026／2027でビルド、単体、standalone、GUI各28項目を検証済みです。2023はこの環境に未インストールのため未検証です。

## 開発・テスト

リポジトリ直下から実行します。Maya devkitとVisual Studioの既存ビルド基盤を使用します。

```powershell
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' tools/build_maya_plugin.py maya/inhouse/hedit --versions 2022 2024 2025 2026 2027
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' maya/inhouse/hedit/tests/run_tests.py 2022 2024 2025 2026 2027
```

テストは補完ユニットテスト、Maya standaloneでの`.mod`／プラグイン／実在する補完候補の確認、同じC++ウィジェットによるoffscreen描画・補完挿入・実行ボタン確認に分かれています。ログ・画像は`.maya-output/hedit-tests/`に保存します。offscreen検証とMaya GUI内での操作確認は別です。

GUI確認は次のコマンドで、既存のMayaとは別の空シーン・専用設定を使用して実施できます。

```powershell
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' maya/inhouse/hedit/tests/run_gui.py 2024
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' maya/inhouse/hedit/tests/run_gui.py 2027
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' maya/inhouse/hedit/tests/run_session.py 2024 2027
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' maya/inhouse/hedit/tests/run_startup.py 2024 2027
```

テスト用モジュールディレクトリにはheditの`.mod`だけを用意します。ワークスペース全体の外部モジュールを読み込んでいた従来の起動タイムアウトは、この構成で解消しました。普段の起動バッチ・ユーザー設定・信頼設定は変更しません。外部ツールを全て有効にした構成の互換性検証とは別です。

GUI内で表示、上下レイアウト、ドッキング／フローティング／再表示、右クリックによる出力消去、コード全体の実行、標準Script Editorとの双方向の出力・変数共有、日本語・警告・例外の表示、実際の補完候補の表示、Enterによる確定、選択範囲だけの実行、閉じる／アンロードを確認します。ショートカットはキーイベントで行編集・Undo/Redo・検索置換・タブ操作・折り返しを確認します。行番号入力、フォルダー/ファイル選択（Qtダイアログ）も自動テストに含めます。OSネイティブのファイルダイアログは別途確認が必要です。結果・画面・候補一覧の画像を`.maya-output/hedit-gui/<日時>/`に保存します。

`run_startup.py`は同じ専用Maya設定を引き継いで3回起動し、右ドック・Python/MEL本文の復元と、閉じた場合の非表示を検証します。共通GUIランナーはuserSetupを無効化するため、復元の入口`startup.initialize()`を起動後に明示実行します。通常環境でのuserSetup自動実行そのものはこのテストの対象外です。

過去の検証結果と変更内容はリポジトリの`WORK_LOG.md`を参照してください。

起動／テストの期限は各120秒、終了待ちは30秒です。`--timeout`と`--shutdown-timeout`で秒数を指定できます。タイムアウト時はテストが起動したプロセスだけを停止します。`result.json`はGUIテスト自体、`monitor.json`は起動／実行／終了の監視、`run-result.json`は両者をまとめた結果です。テスト成功後もMayaが終了しなかった場合は、GUI成功と終了タイムアウトの両方を記録し、終了コードは1になります。

表示される速度計測はキャッシュ済み名前検索だけの所要時間です。初回解析・UI描画・ネットワークドライブを含む操作全体の速度ではありません。


## 0.2.0の使い方

- File → New Python tab / New MEL tabで言語を選んで作成します。右下のPython／MELで現在タブの言語を変更できます。Ctrl+NはPythonタブ、Ctrl+Enterは選択部分（選択なしなら全体）、F5は全体を実行します。言語はタブの自動復元データにも保存します。
- 静的解析は**アクティブなPythonタブだけ**です。MELや非アクティブタブを解析しません。MELは`//`コメントと波括弧のインデントに対応しますが、色分けは簡易表示です。
- File → Open folderでExplorerのルートを置換、Add folderで複数フォルダーを追加します。保存済みファイルを開くと、その親フォルダーとOPEN EDITORSに表示します。ツリーのファイルをダブルクリックして開けます。Removeは表示ルートを外すだけでディスクから削除しません。
- View → Explorer、ツールバーのExplorer、Ctrl+Bでサイドバーを表示/非表示にします。フォルダー一覧と表示状態もタブとともに復元します。対象は.py/.melとフォルダー。展開時に直下だけを読み、シンボリックリンクは辿りません。
- タブが収まらない場合は左右のスクロールボタンを表示します。タブ上のホイールでも隣のタブへ移動します。Ctrl+Tab／Ctrl+Shift+Tabも使用できます。
- Ctrl+F/Ctrl+Hで検索/置換を開きます。暗い背景の2段表示で、Tt・Abc・.*、前後移動、1件/全件置換を操作します。VS Code全機能の再現ではなく、現在タブ内の検索が対象です。

### Maya再起動時の画面復元

復元先はheditのworkspaceControlを明示的に使用し、内部の非表示ログウィンドウへ入らないようにしています。既存ドックを再表示する際は親ホストと本文の両方を表示します。

前回開いていた場合は、Mayaの保存ワークスペースに登録された`uiScript`から画面を再構築します。.modで有効になった`scripts/userSetup.py`からも復元の入口を遅延実行します。ドックの入れ子・タブ位置はMaya標準のワークスペースに保持し、終了通知で現在の配置を保存します。ユーザーが閉じてから終了した場合は自動表示しません。

タブ本文は`tabs.json`、開閉状態・フローティング状態・ワークスペース名は同じフォルダーの`ui.json`に保持します。終了中のUI破棄で「閉じた」扱いに書き換わることを防ぎます。元のMayaワークスペースが削除された場合は厳密な位置を復元できず、以前ドックしていた画面は下側に配置して警告します。SecurityでuserSetup実行を無効化している場合は補助的な自動起動が動きません。Windowメニューまたは`hedit.show()`からも開けます。信頼設定を自動変更する機能はありません。

### Visual Studioプロジェクトだけを生成

```powershell
& 'C:/Program Files/Autodesk/Maya2027/bin/mayapy.exe' tools/build_maya_plugin.py maya/inhouse/hedit --versions 2022 2024 2025 2026 2027 --generate-only
```

`.maya-output/plugin-build/hedit/2024/hedit.sln`または`2027/hedit.sln`をVisual Studioで開きます。通常のビルドでも同じプロジェクトを生成します。`.maya-output/`はGit除外済みで、.sln/.vcxprojは登録しません。構成変更は生成されたプロジェクトではなくCMakeLists.txtへ反映します。

`maya/inhouse/hedit/generate_vs.bat 2027`でも生成できます（年の省略時は2027）。Visual StudioではRelease／x64でビルドします。ロード済み.mllを上書きしないよう、ビルド前に該当Mayaを終了してください。

CMakeを正本にしてIDEプロジェクトを生成する構成は一般的です。Visual Studio自体もこの生成・ビルドの分離に対応しています。[Microsoft公式説明](https://learn.microsoft.com/en-us/cpp/build/cmake-projects-in-visual-studio?view=msvc-180)。

C++の説明は[内製C++コメント規約](../../../docs/cpp-documentation.md)に従う日本語Doxygen形式です。ヘッダーの引数・戻り値、実装の所有権やイベント順序を合わせて読めます。PythonはGoogle形式docstringを維持します。

## 名称変更

フォルダ、Pythonパッケージ、プラグイン、メニューと画面の名称は小文字の`hedit`です。
起動は`import hedit; hedit.show()`を使用します。
旧版から切り替えるときはMayaを再起動してください。旧保存先`heditor`の未保存タブ・UI状態・設定は、
新保存先`hedit`に対応するファイルがまだない場合だけコピーします。旧データは削除しません。
保存済みワークスペースの旧uiScriptに限り、互換用の`heditor.restore()`を残しています。

## ワークスペース起動時の自動ロード

`maya/maya_core.bat`は`maya/modules`を`MAYA_MODULE_PATH`へ追加しています。
`hedit.mod`がバージョン別バイナリと`scripts`を登録し、`scripts/userSetup.py`がGUI初期化後に
`hedit.startup.initialize()`を実行してプラグインとWindowメニューをロードします。
初回はエディタ画面を開かず、前回開いていた場合だけ画面・タブを復元します。
既にロード済みなら重複ロードしません。Mayaの永続autoload設定は変更しません。
バッチ/standaloneでは自動ロードせず、SecurityでuserSetupが禁止されている場合も迂回しません。

GUI回帰テストは隔離環境で実際の`userSetup.py`を明示実行し、遅延ロードとメニュー登録を確認します。
ユーザー環境の全外部ツールを含む起動バッチ実行とは区別しています。

構文警告はMaya同梱Pythonの仕様に従います。例えば2022のPython 3.7では、定数への`is`比較で新しいPythonと同じ警告は出ません。

## 起動済み画面の開き直し

`hedit.show()`やWindowメニューを繰り返し実行すると、既存の本文を一度閉じてから同じ画面を開き直します。
未保存タブ・編集状態・ドッキング位置は保持します。Pythonのモジュールをreloadした場合も
Maya内の既存ホストを回収するため、新しい画面を重ねて作りません。
この操作はプラグインのアンロードやコードの再読み込みではありません。
